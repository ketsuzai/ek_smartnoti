# 🧠 프로젝트 브레인: 프로젝트 개요

> **최종 업데이트**: 2026-04-19
> **작성자**: AI 어시스턴트 (기획자 검토 필요)

---

## ⚠️ 협업 규칙 (COLLABORATION RULES)

> **이 항목은 모든 작업의 최우선 기준입니다. 반드시 숙지하고 준수하세요.**

### 🔒 수정 금지 파일 목록

아래 파일들은 **특별한 지시가 있지 않는 한 절대 수정하지 않는다.**  
만약 수정이 필요한 상황이 발생하면, **작업 전 반드시 한 번 더 확인 요청한다.**

| 파일명 | 설명 |
|--------|------|
| `member-approval.html` | 가입/승인 관리 |
| `invitation-admin.html` | 초대장 관리 |
| `member-status-admin.html` | 회원 상태 관리 |
| `member-list-admin.html` | 통합회원 조회 |
| `org-admin.html` | 기관관리 |
| `dashboard.html` | 대시보드 |

### 🎯 현재 작업 범위

- **우리는 운영관리 부분만 기획한다.**
- 위 파일들은 이미 완성된 협업의 기준이 되는 산출물이다.
- 신규 작업은 `운영관리` 섹션에 해당하는 새 파일을 생성하는 방식으로 진행한다.

---

## 1. 프로젝트 정보

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 키드키즈 스마트알림장 운영관리 시스템 |
| **버전** | v2.4 (SYSTEM V1.0 로그인 기준) |
| **도메인** | 유아교육기관(어린이집, 유치원) 알림장 서비스의 **운영관리 백오피스** |
| **담당 범위** | 운영관리 일부 (회원관리, 기관관리, 초대관리) |
| **기술 스택** | Vanilla HTML/CSS/JS (SPA가 아닌 MPA 구조), Chart.js |
| **폰트** | Pretendard, JetBrains Mono |
| **디자인 시스템** | 커스텀 CSS Variables 기반 |

---

## 2. 관리자 계층 구조

```
┌─────────────────────────────────────────────────┐
│  통합관리자 (슈퍼관리자)                          │
│  - 운영사(키드키즈) 내부 관리자                    │
│  - 전체 시스템 접근 권한                           │
│  - 기관 승인/반려, 회원 관리, 통계, 운영관리 등     │
│  - 사이드바: 다크 테마 (#1A1D2E) ← 기존            │
├─────────────────────────────────────────────────┤
│  기관관리자                                       │
│  - 어린이집/유치원의 원장 또는 부원장               │
│  - 해당 기관 내 모든 권한 (통합관리자 전용 제외)    │
│  - 초대장(교사+학부모), 기관정보수정 가능           │
│  - 콘텐츠 접근 시 반 필터링 필수                   │
│  - 사이드바: 하늘색 테마 (별도 디자인 톤)           │
├─────────────────────────────────────────────────┤
│  교사관리자                                       │
│  - 기관에 소속된 교사                              │
│  - 담당 반 및 원아 운영                            │
│  - 초대장(담당반 학부모만), 기관정보 조회만 가능    │
│  - 담당 반이 여러 개인 경우 반 선택 후 진행         │
│  - 사이드바: 하늘색 테마 (별도 디자인 톤)           │
└─────────────────────────────────────────────────┘
```

> 📎 **상세 권한 매트릭스** → `.brain/07-ROLE-PERMISSION.md` 참조

---

## 3. 현재 구현된 화면 목록

| # | 파일명 | 제목 | 기능 요약 | 상태 |
|---|--------|------|-----------|------|
| 1 | `index.html` | Premium Login | 관리자 로그인 (admin/1234 테스트) | ✅ 완료 |
| 2 | `src/pages/dashboard.html` | 대시보드 | KPI, DAU/MAU 차트, To-do, 랭킹, 디바이스 | ✅ 완료 |
| 3 | `src/pages/member-list-admin.html` | 통합회원 조회 | 전체 회원 목록 검색/필터/상세 | ✅ 완료 |
| 4 | `src/pages/member-approval.html` | 가입/승인 관리 | 가입 승인/거절/취소 | ✅ 완료 |
| 5 | `src/pages/invitation-admin.html` | 초대장 관리 | 기관별 초대장 발송/관리 | ✅ 완료 |
| 6 | `src/pages/member-status-admin.html` | 회원 상태 관리 | 정상/휴면/탈퇴 등 상태 변경 | ✅ 완료 |
| 7 | `src/pages/org-admin.html` | 기관관리 | 기관 조회/승인/상태 3탭 구성 | ✅ 완료 |
| 8 | `src/pages/oper/operation-notice-board.html` | 알림장 | 알림장 목록·상세·작성 패널 | ✅ 완료 |
| 9 | `src/pages/oper/operation-invitation.html` | 초대 관리 | 학부모/교직원 초대 + 이력 (역할별 분기) | ✅ 완료 |
| 10 | `src/pages/oper/operation-org-info.html` | 기관정보 관리 | 기관 기본정보 조회·수정 (canEdit 분기) | ✅ 완료 |
| 11 | `src/pages/oper/operation-class.html` | 반 관리 | KPI 4개, 카드 그리드, 드로어, 원아관리 연동 | ✅ 완료 |
| 12 | `src/pages/oper/operation-child.html` | 원아관리 | 반별 탭, 테이블, 드로어, 등록·상태변경 모달 | ✅ 완료 |
| 13 | `src/pages/oper/operation-dashboard.html` | 운영관리 대시보드 | KPI, 출석현황, 알림장 발송현황, 역할별 뷰 | ✅ 완료 |
| 14 | `src/pages/oper/operation-org-selector.html` | 기관 선택 팝업 | 기관 카드 그리드, 이중 진입, effectiveRole 패턴 | ✅ 완료 |
| 15 | `src/pages/oper/operation-announcement.html` | 공지사항 | v2 개선: 칩 필터, 드로어 재설계, 읽음현황, 임시보관함 | ✅ 완료 |
| 16 | `src/pages/oper/operation-album.html` | 앨범 | KPI 4개, 그리드/리스트 토글, 앨범 모달 | ✅ 완료 |
| 17 | `src/pages/oper/operation-medicine.html` | 투약의뢰서 관리 | 테이블, 드로어, 처리·거절, 엑셀 내보내기 | ✅ 완료 |
| 18 | `src/pages/oper/operation-meal.html` | 식단표 | 주간 캘린더, 인라인 패널 편집, 영양정보·알레르기 | ✅ 완료 |
| 19 | `src/pages/oper/operation-member.html` | 멤버/승인 | 교사+원아 통합 탭, 승인 워크플로, localStorage 동기화 | ✅ 완료 |
| 20 | `src/pages/oper/operation-graduation.html` | 진급/졸업 | 7열 테이블, 일괄 진급, 졸업 확정 모달, 권한 제어 | ✅ 완료 |

---

## 4. 사이드바 네비게이션 구조

### ⚙️ 구현 방식 (공통 컴포넌트 2종)

운영관리 섹션(`src/pages/oper/`)의 모든 페이지는 **공통 사이드바·헤더 컴포넌트**를 사용한다.

#### `operation-sidebar.js`
- **방식**: IIFE 패턴 — CSS를 `<style>` 태그로 주입하고 `<aside id="appSidebar">` 플레이스홀더에 HTML을 렌더링
- **역할 분기**: `sessionStorage.userRole` (`super` / `org_admin` / `teacher`) 읽어 배지·포털 라벨·배경 자동 전환
- **활성 메뉴**: `location.pathname.split('/').pop()`으로 현재 파일명 감지 → `active` 클래스 자동 적용
- **전역 함수 노출**: `window.changeOrg()`, `window.exitContext()`, `window.logout()`

#### `operation-header.js` *(2026-04-19 신규)*
- **방식**: IIFE 패턴 — `<div id="topbarRight">` 플레이스홀더에 헤더 우측 영역 렌더링
- **렌더링 요소**: `[YYYY.MM.DD]` 날짜 · `[📂 임시보관함(N)]` · `[🔔]` · `[⚙️]`
- **글로벌 임시보관함 모달**: 공지사항·알림장·앨범 3종 draft를 타입 배지(파란/초록/보라)로 통합 표시
- **전역 함수 노출**: `window.openGlobalDraftModal()`, `window.closeGlobalDraftModal()`
- **Mock 데이터**: 공지사항 2건, 알림장 2건, 앨범 2건 (하드코딩)

- **사용법**:
  ```html
  <head>
    <script src="operation-sidebar.js" defer></script>
    <script src="operation-header.js" defer></script>
  </head>
  <body>
    <aside id="appSidebar"></aside>
    ...
    <div class="topbar-right" id="topbarRight"></div>
  </body>
  ```

> 📌 향후 신규 `operation-*.html` 페이지는 per-page 사이드바·헤더 코드 없이 위 스크립트 태그 2개만 추가하면 됨.

---

### 4.1 통합관리자 (다크 테마 #1A1D2E)

```
📁 메인
  └── 🏠 대시보드 ─── dashboard.html

📁 관리
  └── 👥 회원/승인관리
      ├── 통합회원 조회 ─── member-list-admin.html
      ├── 가입 승인 관리 ─── member-approval.html (뱃지: 3건)
      ├── 초대 관리 ─── invitation-admin.html
      └── 회원 상태 관리 ─── member-status-admin.html
  └── 🏢 기관관리
      ├── 기관 조회 ─── org-admin.html
      ├── 기관 승인 관리 ─── org-admin.html#approval (뱃지: 5건)
      └── 기관 상태 관리 ─── org-admin.html#status

📁 운영관리 (통합관리자 전용)
  └── 🏫 기관 선택 팝업 → 해당 기관 권한으로 접속
      ├── 기관정보 관리
      ├── 반 관리
      ├── 원아 관리
      ├── 알림장
      ├── 공지사항
      ├── 앨범
      ├── 일정 관리
      ├── 상담 관리
      └── 투약의뢰서 관리

📁 통계관리
  └── 📊 통계 대시보드 ─── dashboard.html (전체 통계)
```

### 4.2 기관관리자 (하늘색 테마)

```
📁 메인
  (대시보드 없음)

📁 기관관리
  └── 🏢 기관정보 관리 ─── 해당 기관 정보 수정 가능

📁 초대 관리
  └── 📨 초대장 관리 ─── 교사 + 학부모 초대 모두 가능

📁 콘텐츠 관리 (반 필터링 필수)
  ├── 반 관리
  ├── 원아 관리
  ├── 알림장
  ├── 공지사항
  ├── 앨범
  ├── 일정 관리
  ├── 상담 관리
  └── 투약의뢰서 관리

📁 통계관리
  └── 📊 해당 기관 통계만 조회
```

### 4.3 교사관리자 (하늘색 테마)

```
📁 기관관리
  └── 🏢 기관정보 관리 ─── 조회만 가능 (수정 불가)

📁 초대 관리
  └── 📨 초대장 관리 ─── 담당 반 학부모 초대만 가능

📁 콘텐츠 관리 (담당 반 기준, 복수 반 시 선택)
  ├── 반 관리
  ├── 원아 관리
  ├── 알림장
  ├── 공지사항
  ├── 앨범
  ├── 일정 관리
  ├── 상담 관리
  └── 투약의뢰서 관리

📁 통계관리
  └── 📊 담당 반 통계만 조회 (복수 반 시 반 선택)
```

---

## 5. 로그인 정책

| 항목 | 내용 |
|------|------|
| **테스트 계정 (통합관리자)** | ID: `admin` / PW: `1234` |
| **테스트 계정 (기관관리자)** | ID: `org_admin` / PW: `1234` |
| **테스트 계정 (교사관리자)** | ID: `teacher` / PW: `1234` |
| **로그인 유지** | 체크박스 존재 (미구현) |
| **인증 방식** | 클라이언트사이드 하드코딩 (프로토타입) |
| **로그인 성공 시 (통합관리자)** | `dashboard.html`로 리다이렉트 |
| **로그인 성공 시 (기관관리자)** | 기관 콘텐츠 메인으로 리다이렉트 (미정) |
| **로그인 성공 시 (교사관리자)** | 기관 콘텐츠 메인으로 리다이렉트 (미정) |
| **실패 시** | 에러 토스트 표시 (3초 후 자동 닫힘) |
| **하단 테스트 계정 표시** | 3가지 계정 모두 표시 (통합관리자 / 기관관리자 / 교사관리자) |

---

## 6. 공통 UI 패턴

- **사이드바**: 240px 고정 — 통합관리자: 다크 테마(#1A1D2E) / 운영관리·기관·교사: 하늘색 테마(#0C2D48). 운영관리 섹션은 `operation-sidebar.js` 공통 컴포넌트로 관리
- **헤더 우측(topbarRight)**: 날짜 · 글로벌 임시보관함 · 알림 · 설정. 운영관리 섹션은 `operation-header.js` 공통 컴포넌트로 관리 (per-page `renderTopbarRight()` 제거)
- **상단바**: 60px 높이, 브레드크럼 + 알림/프로필
- **카드 기반 레이아웃**: border-radius 12~16px, shadow 계층
- **상태 뱃지 시스템**: 도트(●) + 배경색 + 텍스트 색상 조합
- **테이블**: 체크박스 + 필터바 + 검색 + 정렬 패턴
- **모달/패널**: 우측 슬라이드 패널 또는 중앙 모달
- **토스트 알림**: 하단 우측, 자동 소멸
